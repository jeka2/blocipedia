<hi><%= @wiki.title %></hi>

<%= link_to "Edit Topic", edit_wiki_path, class: 'btn btn-warning' %>
<%= link_to "Delete Topic", @wiki, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this article?' } %>

<div class="col-md-8">
    <p><%= @wiki.body %></p>
  </div>
